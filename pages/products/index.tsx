import { Box, Divider, Typography } from "@mui/material";

export default function Products() {
    return <Box sx={{ p: 2 }}>
        <Typography variant="h4" sx={{textAlign: 'center'}}>Product Categories</Typography>
    </Box>
}